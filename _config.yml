# site settings
title: hreikin
subtitle: "- just another dev's site"
email: hreikin@gmail.com
description: This is my Dev site, i made it so you can get to know me, <br />it is made with Jekyll and the wonderful Forty theme.
baseurl: "/hreikin-website" # the subpath of your site, e.g. /blog
url: https://hreikin.gitlab.io # http://localhost:4000 # the base hostname & protocol for your site, https://hreikin.co.uk
author: hreikin
street_address:
city:
state:
zip_code:
country: Scotland
alias: "@hreikin"
timezone: Europe/London
post_excerpt: <!--more-->

# homepage tiles
tiles-source: pages # accepts "posts" or "pages"
tiles-count: 6

# This will require each of these plugins automatically.
plugins:
  - jekyll-responsive-image

# social settings (key must match name of font awesome icon)
# see https://fontawesome.com/icons?d=gallery&p=2&s=brands
# Icons are not shown if value is empty
# You need to restart jekyll for changes to have an effect
socials:
  GitHub: https://github.com/hreikin
  GitLab: https://gitlab.com/hreikin
  Stack-Overflow: https://stackoverflow.com/story/hreikin
  Mastodon: https://glasgow.social/@hreikin
  Google-Plus:
  Instagram:
  LinkedIn:
  Twitter:
  Facebook:
  Slack:

# build settings
markdown: kramdown

sass:
  style: compressed

exclude:
- "*.gemspec"
- THEME-LICENSE.md
- THEME-CONTRIBUTING.md
- THEME-README.md
- README.md
- CREDITS.md
- theme-landing.md
- theme-generic.md
- theme-all_posts.md
- theme-elements.md

# jekyll-responsive-images configuration
responsive_image:
  # [Required]
  # Path to the image template.
  template: _includes/responsive-image.html #THIS ONE
# [Optional, Default: 85]
  # Quality to use when resizing images.
  default_quality: 90
# [Optional, Default: []]
  # An array of resize configuration objects. Each object must contain at least
  # a `width` value.
  sizes:
    - width: 480  # [Required] How wide the resized image will be.
      quality: 80 # [Optional] Overrides default_quality for this size.
    - width: 800
    - width: 1400
      quality: 90
# [Optional, Default: false]
  # Rotate resized images depending on their EXIF rotation attribute. Useful for
  # working with JPGs directly from digital cameras and smartphones
  auto_rotate: false
# [Optional, Default: false]
  # Strip EXIF and other JPEG profiles. Helps to minimize JPEG size and win friends
  # at Google PageSpeed.
  strip: true
# [Optional, Default: assets]
  # The base directory where assets are stored. This is used to determine the
  # `dirname` value in `output_path_format` below.
  base_path: assets
# [Optional, Default: assets/resized/%{filename}-%{width}x%{height}.%{extension}]
  # The template used when generating filenames for resized images. Must be a
  # relative path.
  #
  # Parameters available are:
  #   %{dirname}     Directory of the file relative to `base_path` (assets/sub/dir/some-file.jpg => sub/dir)
  #   %{basename}    Basename of the file (assets/some-file.jpg => some-file.jpg)
  #   %{filename}    Basename without the extension (assets/some-file.jpg => some-file)
  #   %{extension}   Extension of the file (assets/some-file.jpg => jpg)
  #   %{width}       Width of the resized image
  #   %{height}      Height of the resized image
  #
  output_path_format: assets/images/resized/%{width}/%{basename}
# [Optional, Default: true]
  # Whether or not to save the generated assets into the source folder.
  save_to_source: false
# [Optional, Default: false]
  # Cache the result of {% responsive_image %} and {% responsive_image_block %}
  # tags. See the "Caching" section of the README for more information.
  cache: false
# [Optional, Default: []]
  # By default, only images referenced by the responsive_image and responsive_image_block
  # tags are resized. Here you can set a list of paths or path globs to resize other
  # images. This is useful for resizing images which will be referenced from stylesheets.
  extra_images:
    - assets/*/*/*/*.{jpeg,jpg,png}